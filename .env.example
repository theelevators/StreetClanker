# Environment

Copy to `.env` if you want overrides. Nothing here is required for local `npm run dev`.

```bash
# API + production static port (default 8787)
PORT=8787

# production = serve Vite build from Express
# NODE_ENV=production
```
